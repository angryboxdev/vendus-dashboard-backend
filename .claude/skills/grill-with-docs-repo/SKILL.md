---
name: grill-with-docs-repo
description: Same as grill-with-docs, but keeps the docs it produces inside this repo's module READMEs.
disable-model-invocation: true
---

Run a `/grilling` session, using `domain-modeling-repo` skill instead of `domain-modeling`.
